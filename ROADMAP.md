# FREE TIMES — 開発メモ / 保存記録

> 新しいセッションでもここを見れば思い出せるようにするメモ。
> **最終更新：2026-06-19**

## 保存ポイント（スナップショット）
- **公開前の状態を専用ブランチで保存：`snapshot-prepublish-2026-06`**（コミット `f760f7a`）。後で必ずここに戻れる。
- 復元したいときは、このブランチ／コミットの `index.html` 等に戻す。
- （この環境では git タグの push が 403 で不可のため、タグではなくブランチで固定している。）

## このサイトの基本
- 公開サイト: https://hodakaman5.github.io/freetimes/
- **GitHub Pages の公開元ブランチ = `claude/external-homepage-editing-xvvk8i`**（このブランチ）。
  ここに push すると自動でビルド・公開される。`main` は別系統なので注意。
- 単一の `index.html`（静的サイト・サーバー/DBなし）。
- 画像：`hero.png` / `paper1〜3.png`（スライド等）、`henkyokusha.jpg`（追加記事）。

---

## ★ これから付ける機能（保留中・本人指示）
1. **「公開する」ボタン（✅ 実装済み 2026-06-25）**
   - 編集モードのパネルに **🚀公開する** ＋ **🔑公開の設定** を追加。本人が iPad/携帯から編集 → ボタンで**全員に反映**できる。
   - 仕組み：「公開する」で今の `content` を **`content.json`** として GitHub（公開元ブランチ）へコミット（画像の dataURL は `img/u<hash>.<ext>` として自動アップロード→パス参照に置換）。サイトは起動時に `content.json` を fetch して土台に採用（無ければ DEFAULT_CONTENT に自動フォールバック）。デザイン/機能＝`index.html`（こちらが編集）と分離。
   - 認証：**GitHub トークン**を「🔑公開の設定」で1回登録（localStorage キー `freetimes_gh_token`。公開コードには出ない）。classic PAT（repo / 90日）で運用中。`Authorization: Bearer` 方式。fine-grained 作成は iPad Safari で確認画面が反応せず断念→classic を採用。
   - 定数：`GH_OWNER=hodakaman5 / GH_REPO=freetimes / GH_BRANCH=claude/external-homepage-editing-xvvk8i`。
   - 安全：公開前に confirm、前版は Git 履歴で復元可、パスコードはそのままなので公開ボタン有無と一般公開は別。トークン流出時は GitHub 側で Delete→作り直し。
2. **Twitter/X 連携**：公式Xアカウントのリンクを全ページ**フッター**に置く（URL未提供で保留）。
3. **Instagram 連携**：後回し。
4. **ダイレクトメール（問い合わせ）機能**：最終目標。仕様は実装時に確認。
5. **アーカイブのページ分け／「もっと見る」**：記事が数百件規模になったら追加（軽さ維持）。

---

## 外部から編集・公開する方法（将来の選択肢・本人保留）
> 本人意向（2026-06-19）：「最終的にはこのどれかで外部編集・公開する。今はまだ公開しない。**保存しておくこと**。」

**重要：いまの index.html（デザイン・全機能・演出）はそのまま引き継げる。作り直し不要。**
変わるのは「編集のやり方・記事データの置き場所」だけで、見た目は不変。移行前に編集モードの **⬇ バックアップ(JSON)** を取っておく。

| 方法 | 見た目/機能 | 改修量 | メモ |
|---|---|---|---|
| ① 公開ボタン＋GitHubトークン | そのまま | ほぼ無し | 今の ?edit 編集画面を活かす。本命（「これから付ける機能1」と同じ）。 |
| ② GitHub直接編集（github.com / アプリ / github.dev） | そのまま | 無し | 設定ゼロ・どの端末でも。ただし生コード編集で技術寄り。 |
| ③ Netlifyへ引っ越し＋Netlify Identity＋Decap CMS | そのまま（URL変わる） | 小〜中 | ログイン式の管理画面。運用がラク。 |
| ④ Decap/Sveltia CMS（GitHub Pagesのまま） | そのまま | 中 | admin画面で編集→自動公開。要OAuth設定。 |
| ⑤ Googleスプレッドシート/Notion等から流し込み | そのまま | 中 | 慣れたアプリで執筆。画像はリンク管理＝文章向き。 |

- おすすめ：今の自作エディタを残すなら**①**、本格運用なら**③（Netlify＋Decap）**、手間ゼロなら**②**。
- どれを選んでも index.html は流用OK。公開に踏み切るとき本人が方式を指定 → こちらで手順込み実装。

---

## 現状の実装内容（公開前スナップショット時点）

### 見た目・構成
- ロゴ（ヘッダー＆フッター3か所）= Nasalization風の**白いワーム風 SVG**（白グロー）。色は緑→青→白で調整し**現状は白**。
- ヒーロー：画像スライド（Ken Burns）、速報ティッカー＋ライブ時計。
- ナビ：Top / Archive / Search。検索はタイトル検索＋カテゴリ絞り込み。
- 記事モーダル：前後送り・キーボード操作対応。
- 記事カード画像は `object-fit:contain`（全体表示・切れない）。Featured/Archive/Search 共通。
- フッター3ページ統一、著作権 © 2026。

### 月刊号
- トップでは**コンパクトな 3:1 バナー**表示、**タップでモーダル**。
- 画像2枚対応：**1枚目=バナー**、**2枚目=タップ後の記事内**。
- **タップ後は2枚目だけ表示**（1枚目バナーは出さない）。並びは（2枚目）画像→テキスト。

### 編集モード（?edit）
- URL に `?edit` を付けると右下に「✏ 編集する」（本人だけの合言葉）。
- 文章/画像/スライド/ティッカー/フッター/テーマ色の編集、記事の追加・削除、月刊号編集、バックアップ(JSON)・読み込み・初期化。
- **記事カードを並べ替え可能**：各カード右上の ⠿ ハンドルを押してドラッグ（本体長押しでも可）。`setPointerCapture`使用でiPadのデスクトップ表示でも動作。
- 保存は **localStorage（その端末だけ）**。合計約5MBまで。**全体公開は別途必要**（上の「公開する」ボタン or 本人がチャットで指示）。

### データの仕組み（重要）
- `DEFAULT_CONTENT` がベース。`mergeContent` は **localStorage の内容を優先**しつつ、**保存データに無い id の記事は必ず追加表示**する。
- **追加記事の id は負の数（例 -1, -2…）** を使う（エディタ採番 Math.max(0,...)+1 と衝突せず、保存データのある端末でも必ず表示されるため）。
- 既存の追加記事：`id:-1`「均衡を壊す"偏極者" 存在浮上！！」(`henkyokusha.jpg`)。

### コンテンツ運用ルール
- 残す記事・画像はチャットで受け取り、画像は JPEG 約400KB前後に圧縮して repo に入れる。
- 公開サイト（repo）なら画像は数百記事規模でも問題なし。
- 編集モード(localStorage)は少量・その端末のみ＝長期アーカイブ不向き。

### 解析（閲覧がわかる機能）
- **本人決定（2026-06-25）：「完成・公開する時につける。今は保留」。** それまで触らない。
- 方式は **GoatCounter（無料・Cookieなし・プライバシー配慮）** で確定。`index.html` 末尾にコメントアウト済み（`data-goatcounter="https://freetimes.goatcounter.com/count"` の2行）。
- **有効化手順（その時が来たら）**：
  1. 本人が goatcounter.com で無料登録（**コード名＝`freetimes`** にする → ダッシュボードは `freetimes.goatcounter.com`／メール・パスワードを設定）。
  2. `index.html` 末尾のGoatCounter 2行の **コメント（`<!-- -->`）を外す** → 公開（🚀 or push）。
  3. 以後 `freetimes.goatcounter.com` で「閲覧回数・日時・ページ・おおよその参照元/国」が見られる（**誰が見たかは取得しない＝匿名**）。
- 代替案：ページ上に閲覧数を出す方式（👁 表示）もあるが、無料カウンターが不安定なため非推奨。本人にもA(GoatCounter)を推奨済み。

---

## 公開・反映の関係（混同しやすい点）
- **編集モードの編集（iPad/携帯）= その端末だけ**。他人・他端末・全体には出ない。
- **全体公開**＝ repo に push（＝チャットで指示 → こちらが反映、または将来の「公開する」ボタン）。
