# FREE TIMES — 開発メモ / TODO

> 新しいセッションでもここを見れば思い出せるようにするメモ。

## このサイトの基本
- 公開サイト: https://hodakaman5.github.io/freetimes/
- **GitHub Pages の公開元ブランチ = `claude/external-homepage-editing-xvvk8i`**（このブランチ）。
  ここに push すると自動でビルド・公開される。`main` は別系統なので注意。
- 単一の `index.html`（静的サイト・サーバー/DBなし）。

## ★ 保留中の重要タスク（本人の指示で先送り）
- **「公開する」ボタンの実装**：本人が「公開直前まではこのままでいい。公開する時にこの機能を付ける」と決定（2026-06）。
  - 内容：編集モード（?edit）で iPad/携帯から編集 → そのまま**全員に反映**できるようにする。
  - 方式候補（おすすめ順）：
    1. 編集モードに「公開する」ボタンを追加し、GitHub のトークン（本人が一度登録）で repo に直接コミット → 自動公開。
    2. Decap CMS などの git ベース CMS を導入。
  - **本人から「公開する時が来た」と言われたら着手すること。**

## 編集・公開の仕組み（重要）
- 編集モードは URL に `?edit` を付けると右下に「✏ 編集する」が出る（本人だけの合言葉）。
- 編集内容は **そのブラウザの localStorage に保存 → その端末だけ**。他人・他端末・全体には反映されない。
- localStorage は合計 約5MB まで（画像数枚で限界）。長期アーカイブには不向き。
- **全体に公開する正規ルート**：本人が画像/文章をチャットに貼る → こちらで圧縮し `DEFAULT_CONTENT` に追加して push（＝全員に反映）。

## コンテンツ運用ルール
- 残す記事・画像はチャットで受け取り、画像は JPEG 約400KB前後に圧縮して repo に入れる。
- 追加記事は `DEFAULT_CONTENT.articles` に入れる。**id は負の数（例 -1, -2…）**を使う。
  - 理由：`mergeContent` は localStorage の記事を優先しつつ、保存データに無い id の記事を追加表示する。
    負の id はエディタ採番（Math.max(0,...)+1）と衝突しないので、保存データのある端末でも必ず表示される。
- 記事カード画像は `object-fit:contain`（全体表示・切れない）。

## これまでの主な変更（多機能版で実施済み）
- ロゴ（ヘッダー＆フッター）を Nasalization 風の **白いワーム風 SVG**（グローつき）に。色は過去に緑→青→白で調整、現状は白。
- 月刊号はトップでは**コンパクトな 3:1 バナー**表示・タップでモーダル。
  - 画像2枚対応：1枚目=バナー、2枚目=タップ後の記事内。**タップ後は2枚目だけ表示**（1枚目は出さない）。
  - モーダルの並び：（2枚目）画像 → テキスト。
- アクセス解析（GoatCounter）はコメントアウトで保留中。

## 将来やりたい
- Twitter/X 連携（フッターにリンク予定、URL未提供で保留）。
- Instagram 連携（後回し）。
- アーカイブが数百件規模になったらページ分け／「もっと見る」を追加。
