# FREE TIMES — 開発メモ / 保存記録

> 新しいセッションでもここを見れば思い出せるようにするメモ。
> **最終更新：2026-06-17（公開前スナップショット）**

## 保存ポイント（スナップショット）
- **公開前の状態を専用ブランチで保存：`snapshot-prepublish-2026-06`**（コミット `f760f7a`）。後で必ずここに戻れる。
- 復元したいときは、このブランチ／コミットの `index.html` 等に戻す。
- （この環境では git タグの push が 403 で不可のため、タグではなくブランチで固定している。）

## このサイトの基本
- 公開サイト: https://hodakaman5.github.io/freetimes/
- **GitHub Pages の公開元ブランチ = `claude/external-homepage-editing-xvvk8i`**（このブランチ）。
  ここに push すると自動でビルド・公開される。`main` は別系統なので注意。
- 単一の `index.html`（静的サイト・サーバー/DBなし）。
- 画像：`hero.png` / `paper1〜3.png`（スライド等）、`henkyokusha.jpg`（追加記事）。

---

## ★ これから付ける機能（保留中・本人指示）
1. **「公開する」ボタン（最優先・公開直前に実装）**
   - 本人決定：「公開直前まではこのまま。公開する時にこの機能を付ける」（2026-06）。
   - 内容：編集モード（?edit）で iPad/携帯から編集 → そのまま**全員に反映**。
   - 方式候補（おすすめ順）：
     1. 編集モードに「公開する」ボタンを追加し、GitHub トークン（本人が一度登録）で repo に直接コミット → 自動公開。
     2. Decap CMS などの git ベース CMS を導入。
   - **本人から「公開する時が来た」と言われたら着手。**
2. **Twitter/X 連携**：公式Xアカウントのリンクを全ページ**フッター**に置く（URL未提供で保留）。
3. **Instagram 連携**：後回し。
4. **ダイレクトメール（問い合わせ）機能**：最終目標。仕様は実装時に確認。
5. **アーカイブのページ分け／「もっと見る」**：記事が数百件規模になったら追加（軽さ維持）。

---

## 現状の実装内容（公開前スナップショット時点）
### 見た目・構成
- ロゴ（ヘッダー＆フッター3か所）= Nasalization風の**白いワーム風 SVG**（白グロー）。色は緑→青→白で調整し**現状は白**。
- ヒーロー：画像スライド（Ken Burns）、速報ティッカー＋ライブ時計。
- ナビ：Top / Archive / Search。検索はタイトル検索＋カテゴリ絞り込み。
- 記事モーダル：前後送り・キーボード操作対応。
- 記事カード画像は `object-fit:contain`（全体表示・切れない）。Featured/Archive/Search 共通。
- フッター3ページ統一、著作権 © 2026。

### 月刊号
- トップでは**コンパクトな 3:1 バナー**表示、**タップでモーダル**。
- 画像2枚対応：**1枚目=バナー**、**2枚目=タップ後の記事内**。
- **タップ後は2枚目だけ表示**（1枚目バナーは出さない）。並びは（2枚目）画像→テキスト。

### 編集モード（?edit）
- URL に `?edit` を付けると右下に「✏ 編集する」（本人だけの合言葉）。
- 文章/画像/スライド/ティッカー/フッター/テーマ色の編集、記事の追加・削除、月刊号編集、バックアップ(JSON)・読み込み・初期化。
- **記事カードを並べ替え可能**：各カード右上の ⠿ ハンドルを押してドラッグ（本体長押しでも可）。`setPointerCapture`使用でiPadのデスクトップ表示でも動作。
- 保存は **localStorage（その端末だけ）**。合計約5MBまで。**全体公開は別途必要**（上の「公開する」ボタン or 本人がチャットで指示）。

### データの仕組み（重要）
- `DEFAULT_CONTENT` がベース。`mergeContent` は **localStorage の内容を優先**しつつ、**保存データに無い id の記事は必ず追加表示**する。
- **追加記事の id は負の数（例 -1, -2…）** を使う（エディタ採番 Math.max(0,...)+1 と衝突せず、保存データのある端末でも必ず表示されるため）。
- 既存の追加記事：`id:-1`「均衡を壊す"偏極者" 存在浮上！！」(`henkyokusha.jpg`)。

### コンテンツ運用ルール
- 残す記事・画像はチャットで受け取り、画像は JPEG 約400KB前後に圧縮して repo に入れる。
- 公開サイト（repo）なら画像は数百記事規模でも問題なし。
- 編集モード(localStorage)は少量・その端末のみ＝長期アーカイブ不向き。

### 解析
- アクセス解析（GoatCounter）はコメントアウトで保留中。

---

## 公開・反映の関係（混同しやすい点）
- **編集モードの編集（iPad/携帯）= その端末だけ**。他人・他端末・全体には出ない。
- **全体公開**＝ repo に push（＝チャットで指示 → こちらが反映、または将来の「公開する」ボタン）。
